name: pr-claude-code-review

on:
  pull_request:
    branches: ["main"]

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run review
        run: echo "Claude Code review placeholder"
